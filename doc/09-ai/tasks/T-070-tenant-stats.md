# T-070 — GET /api/v1/tenants/{slug}/stats — estadísticas del tenant

**Estado:** 🔲 PENDING
**Horizonte:** mediano plazo
**Módulos afectados:** `keygo-app`, `keygo-api`, `keygo-supabase`

## Requisito

Implementar `GET /api/v1/tenants/{slug}/stats` para rol `ADMIN_TENANT`, devolviendo:
usuarios (por estado), apps (total), memberships (total/activas), sesiones activas.

## Pasos de Implementación

> A detallar cuando se active la tarea.

## Verificación

> A definir cuando se active la tarea.
