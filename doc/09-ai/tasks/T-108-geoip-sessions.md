# T-108 — Geolocalización por IP en sesiones

**Estado:** 🔲 PENDING
**Horizonte:** mediano plazo
**Módulos afectados:** `keygo-app`, `keygo-infra`, `keygo-run`

## Requisito

Enriquecer sesiones con geolocalización por IP usando `GeoIpPort` + adapter
(MaxMind/ip-api). Agregar campo `location` en `AccountSessionData`.
Feature flag: `keygo.session.geoip.enabled=false`.

## Pasos de Implementación

> A detallar cuando se active la tarea.

## Verificación

> A definir cuando se active la tarea.
