# T-061 — Externalizar lista de orígenes CORS por ambiente

**Estado:** 🔲 PENDING
**Horizonte:** corto plazo
**Módulos afectados:** `keygo-run`, docs

## Requisito

Externalizar la lista de orígenes CORS permitidos por ambiente, documentar
`KEYGO_CORS_ALLOWED_ORIGINS_0` en `.env.example` y en `doc/07-operations/environment-setup.md`.
El perfil `prod` debe usar lista vacía (denegación por defecto).

## Pasos de Implementación

> A detallar cuando se active la tarea.

## Verificación

> A definir cuando se active la tarea.
