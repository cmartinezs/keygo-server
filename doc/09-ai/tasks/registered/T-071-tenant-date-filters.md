# T-071 — Filtros created_after / created_before en GET /api/v1/tenants

**Estado:** 🔲 PENDING
**Horizonte:** mediano plazo
**Módulos afectados:** `keygo-app`, `keygo-api`, `keygo-supabase`

## Requisito

Agregar filtros `created_after` y `created_before` al endpoint `GET /api/v1/tenants`
y al use case `ListTenantsUseCase` / `TenantFilter` para análisis de crecimiento temporal.

## Pasos de Implementación

> A detallar cuando se active la tarea.

## Verificación

> A definir cuando se active la tarea.
