# T-051 — Suite de autorización por endpoint con matriz rol/tenant

**Estado:** 🔲 PENDING
**Horizonte:** corto plazo
**Módulos afectados:** `keygo-api`, `keygo-run`

## Requisito

Crear una suite de tests de autorización por endpoint usando `@PreAuthorize`,
con una matriz que cubra combinaciones rol/tenant: `ADMIN`, `ADMIN_TENANT` (match
y mismatch), `USER_TENANT`. El objetivo es tener cobertura explícita de las reglas
de acceso por recurso.

## Pasos de Implementación

> A detallar cuando se active la tarea.

## Verificación

> A definir cuando se active la tarea.
